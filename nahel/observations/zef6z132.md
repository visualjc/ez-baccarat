---
id: zef6z132
name: decision-y6344y07
created: 2026-08-26T22:29:20Z
tags:
  - decision
  - task
sources:
  - 6pwb0b9k
  - zd8xcmpc
  - ckvgadva
---
Reserve the rotated card's painted width with margin-inline: calc((var(--card-h) - var(--card-w)) / 2), and KEEP the translate nudge — mirrored per seat — unless looking at the result shows it was only overlap compensation.

Decided by resolving task ticket y6344y07, charting: Both hands are seated against the centre line the way a live EZ Baccarat layout is dealt: each seat's two-card group holds its INNER edge the same measured distance off the divider, each seat's total rides beside the divider instead of at the felt's outer edge, and each sideways third card lies OUTBOARD of its own group — the player's to its left, per the casino reference photo. A fresh agent can tell it has arrived by measuring: player inner edge to centre line equals banker inner edge to centre line, and both totals are nearer the divider than the felt's rim.

Question:
How does the sideways third card get room, given rotate(90deg) leaves its layout box at card width so its 132px visual overhangs 20px on each side — reserve the space in the flex track, or let it bleed outboard toward the felt rim?

Rationale:
rotate() does not change the layout box, so a rotated 92x132 card occupies a 92px flex track while painting 132px wide: 20px of overhang each side. Symmetric inline margins make the row reserve what the card actually paints, which is the defect.

What the grill changed, and this one Claude lost. The draft went further and proposed deleting translate(6px, -4px) as 'the overlap compensation it was'. Codex refused it: that purpose was asserted, never verified, and the offset may be optical correction for how a rotated card reads against its upright neighbours rather than a hack around the overlap. Deleting it on theory while the human is AFK and cannot say 'that looks wrong' is not a defensible trade. So: margins go in, the nudge stays, its horizontal component mirrors with the seat, and it is re-tuned only against an actual screenshot — the cheap, reversible order.

The vertical question codex raised resolves on its own: the rotated card paints 92px tall inside a min-height 132px row whose align-items is center, so it stays optically centred against the upright pair without intervention.

Whatever the nudge ends up as, it must be re-stated identically inside every third-card-emphasis keyframe stop — see ticket ymggceax, and the resolved research ticket czkdja9x for why the keyframes restate it at all.

What would overturn this: a screenshot showing the nudge fighting the new margins, which is exactly the check this decision defers to rather than guesses at.
