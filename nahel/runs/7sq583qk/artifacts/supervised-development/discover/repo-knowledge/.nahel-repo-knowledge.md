Repo-knowledge report, read-only. CodeGraph was unavailable (`command not found`), so targeted `rg` was used.

- `src/ui/bankroll.ts:38-85` owns bankroll DOM and `ezbac.bankroll` persistence. Existing unused `reset()` sets and persists `DEFAULT_BANKROLL` (`1000`).
- `src/ui/table-view.ts:131-146` mounts bankroll and closes the wager guard over `bankroll.get()`.
- `src/ui/game.ts:75-91` recalculates deal/rebet/2x availability; reload must call `syncControls()`.
- `src/ui/game.ts:164-203` replaces the engine and emits `shoe:opened`; reload must not use this path.
- Engine state owns shoe and counts in `src/engine/engine.ts:40-48`; only `dealRound()` mutates them at `137-147`.
- Count panel resets only on `shoe:opened` at `src/ui/count-panel.ts:76-83`; a bankroll-only action must not emit that event.
- Tray/bankroll styling is `src/styles/app.css:698-746`; mobile stacking is `1145-1161`.
- Recommended seam: guarded reload visible only at zero, reuse current persistence path, require `!busy`, call `syncControls()`, announce refill, and emit only existing `bankroll:changed` if needed.
- Global keyboard handling can intercept Space/Enter, so the native button needs focused keyboard evidence and may need propagation handling.
- Focused tests: stored zero action and exact storage update; positive/no-repeat guard; throwing storage; game-state preservation; browser Tab+Enter, continued betting on same shoe, persistence, and responsive layout.
- Likely files: `src/ui/bankroll.ts`, `src/ui/game.ts`, `src/styles/app.css`, focused bankroll test; table/keyboard files only if the seam requires them. No engine/count/math changes.
