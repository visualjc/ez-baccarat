Context report for item `0mg270yr`:

- Bankroll is play chips only. It starts at `$1,000`, persists under `ezbac.bankroll`, clamps stored negatives to zero, and tolerates blocked or throwing storage.
- Shoe and count state are in-memory engine state and are not persisted. Do not change that behavior.
- At exactly `$0`, show a clear player-initiated action to receive another `$1,000`; do not grant chips automatically.
- Activation changes bankroll from `$0` to `$1,000`, refreshes betting controls, allows play again, remains mouse/keyboard operable, and announces status.
- Do not expose or enable the action while bankroll is positive or settlement is busy.
- Preserve engine identity, shoe cursor/cards remaining, round, seed, burn state, Dragon/Panda counts and trace, mode, and wager history. Do not emit shoe/count events or run new-shoe flow.
- Persist through existing storage behavior; do not touch `ezbac.chip`, `ezbac.rules`, or add shoe/count persistence.
- Focused checks: zero vs positive mount, exact one-time reload, storage write, storage failure, game-state preservation, rebet control refresh, and browser continuation on same shoe.
- Existing `BankrollHandle.reset()` is unconditional and does not notify game controls; settlement reaches zero while busy; page reload naturally creates a fresh shoe, which is distinct from live reload preservation.
- Branch is `codex/playbook-dogfood-add-funds`; no feature code existed at discovery time.
