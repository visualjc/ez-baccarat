Context-gatherer report, read-only.

- Native mouse, Enter, and Space must each grant exactly once from fresh settled zero, with one existing bankroll write, refreshed controls, hidden action, exact live announcement, and no Deal. Keyboard proof cannot manually dispatch a follow-up click.
- At 390x844, the full action and tray must remain visible and usable with no left clipping or substantial document overflow.
- Preserve all prior passing evidence: seed-1 zero flow, mouse refill, 401/397-card same-shoe state and byte-identical count labels through refill, continued same-shoe wagering, repeat refill, and page-reload bankroll persistence.
- Keep exact-zero/positive/busy/repeat/storage-failure guards and the bankroll-only seam. Never replace engine state or emit shoe/count/round events.
- Next controller drive must name the exact SHA, prove native Enter and Space separately without synthetic click, compare shoe/count state, and capture 390x844 width/screenshot evidence. No QA/review/PR readiness claim before that exact-SHA drive.
