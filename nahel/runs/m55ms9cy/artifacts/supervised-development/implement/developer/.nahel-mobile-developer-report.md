# Mobile containment developer report

Candidate: `f22753f88b398340a26f36e655962f19b022b7b5`

Parent: `3116008f596e4dfed7be1f70e26e9ada38078318`

Commit: `fix: contain bankroll action on mobile`

## Files

- `src/ui/table-shell.test.ts`
- `src/styles/app.css`

The parent-to-candidate diff contains exactly these two owned files: five added lines, no removals. No behavior, markup, other CSS region, controller evidence/report, Nahel state, or protected domain module was edited.

## Red/green

- RED: extended the existing max-width 859px contract to require `#table-view { grid-template-columns: minmax(0, 1fr); }`.
- Exact failure on `3116008f`: expected `/grid-template-columns:\s*minmax\(0,\s*1fr\);/`; received only `grid-template-rows: 88px minmax(180px, 1fr) 184px minmax(92px, auto);`.
- GREEN: added only `grid-template-columns: minmax(0, 1fr);` to the existing narrow `#table-view` rule. No overflow masking was added.
- Focused green: `rtk bun test src/ui/table-shell.test.ts` — 4 pass, 0 fail, 27 assertions.

## Checks

- Required regression group: bankroll + table-shell + hand-layout + QA probe — 24 pass, 0 fail, 155 assertions.
- Full `rtk bun test` — 96 pass, 0 fail, 3,633 assertions across 20 files.
- `bunx tsc --noEmit` — pass.
- `rtk npm run build` — pass; 36 modules transformed.
- `git diff --check 3116008f596e4dfed7be1f70e26e9ada38078318` — pass.
- Current-source Nahel validate — 0 errors, 4 existing/state warnings (compaction overdue, prior result frontmatter, and two workflow-drift warnings).
- Current-source Nahel doctor — `contract OK: env complete, healthcheck passed`.
- Post-commit protected paths — clean.

## Browser status

No browser is available in this headless developer task. Browser verification, QA/review, and PR creation are not claimed. The exact candidate must pass the controller gate in `.nahel-mobile-controller-handoff.md` unchanged.

